{
  "name": "loom-board",
  "version": "1.0.0",
  "description": "Loom Board - Kanban task tracker",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp loom-board.html dist/ && cp task-board.json dist/ 2>/dev/null || true"
  }
}