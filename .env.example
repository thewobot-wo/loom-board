# Frontend (Vite)
# Set this to your Convex deployment URL (public).
VITE_CONVEX_URL=https://<your-deployment>.convex.cloud

# Convex CLI (optional, for local dev convenience)
CONVEX_DEPLOYMENT=dev:<your-deployment-name>
CONVEX_URL=https://<your-deployment>.convex.cloud
CONVEX_SITE_URL=https://<your-deployment>.convex.site

# Convex Auth (backend). These are configured on the Convex deployment via:
#   npx convex env set NAME value
# Do not put secrets (e.g. AUTH_GOOGLE_SECRET, JWT_PRIVATE_KEY) in a committed file.
SITE_URL=http://localhost:5173
